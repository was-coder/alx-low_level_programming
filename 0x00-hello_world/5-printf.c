#include <stdio.h>

/**
*main - function needs to return some integers after execution
*Return: returns 0 for success and 1 for fail
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
